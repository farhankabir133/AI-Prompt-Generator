import { PromptCategory, PromptTone, PromptLength } from './types';

export const CATEGORY_OPTIONS = Object.values(PromptCategory);
export const TONE_OPTIONS = Object.values(PromptTone);
export const LENGTH_OPTIONS = Object.values(PromptLength);